*IR
IR is a library for parsing IR remote control signals.
** NEC
Microzig example
#+begin_src go
  pub const microzig_options = microzig.Options{
      .log_level = .debug,
      .logFn = rp2xxx.uart.log,
      .interrupts = .{ .IO_IRQ_BANK0 = .{ .c = callback } },
  };

  var t1: time.Absolute = undefined;
  var t2: time.Absolute = undefined;
  var parser = ir.IR{};

  fn callback() linksection(".ram_text") callconv(.c) void {
      var iter = gpio.IrqEventIter{};
      while (iter.next()) |e| {
          switch (e.pin) {
              button => {
                  togglePower();
              },
  				ir_input => {
                  t2 = rptime.get_time_since_boot();
                  if (parser.put(t2.diff(t1).to_us())) {
                      checkIR();
                  }
                  t1 = t2;
              },
  				else => {},
  			}
      }
  }

  fn checkIR() void {
  	if (parser.value()) |msg| {
  		if (msg.address == 0x35 and msg.command == 0x40) { // minidsp flex on/off button
  			// do something
  		}
  	} else |_| {
  		blink(5);
  	}
  }
#+end_src
*** links
https://www.sbprojects.net/knowledge/ir/nec.php
** RC5 (TODO)
*** links
https://techdocs.altium.com/display/FPGA/Philips+RC5+Infrared+Transmission+Protocol
** Fujitsu
*** links
https://github.com/crankyoldgit/IRremoteESP8266/blob/master/src/ir_Fujitsu.cpp
https://github.com/albertnis/fujitsu-ar-ry13-ir-codes
